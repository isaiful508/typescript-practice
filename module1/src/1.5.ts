//Reference Type --> Object 

const user: {
    firstName : string;
    middleName? : string; //optional type  and it can be read only that never it will assign it will read only
    lastName : string;
    isMarried: boolean;
    company : 'Kodezen' //type --> literal typw
} = {
    firstName : "Saiful",
    lastName : "MOlla",
    isMarried : true,
    company : 'Kodezen'
}

